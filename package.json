{
  "name": "ai-age-verification",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "mkdir public && cp index.html public/index.html && esbuild index.tsx --bundle --outfile=public/index.js --platform=browser --format=esm --jsx=automatic --external:react --external:react-dom/client --external:@mediapipe/tasks-vision"
  },
  "dependencies": {
    "@google/genai": "^1.17.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.5",
    "@types/react": "^18.3.3",
    "@types/react-dom": "^18.3.0",
    "@vercel/node": "^3.0.0",
    "esbuild": "^0.20.2",
    "typescript": "^5.3.3"
  }
}